version: '3.7'

services:

  app:
    image: markus621/golang:1.1.0
    container_name: stagger-appgo
    restart: no
    working_dir: /app
    command: go run cmd/stagger/main.go
    ports:
      - 12000:12000
      - 8080:8080
    volumes:
      - ./../../:/app
